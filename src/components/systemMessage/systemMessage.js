import React from "react";

export default function systemMessage() {
  return (
    <div className="systemMessage">
      <header>
        <h1>systemMessage</h1>
      </header>
    </div>
  );
}
